@model IEnumerable<Domain.Entities.t_siz_manufacturer>

<table class="table table-striped table-bordered table-condensed">
    @foreach (var itm in @Model)
    {
        <tr>
            <td>@itm.name</td>
            @*<td><a href="@Url.Action("DeleteMan", "SIZ", new { id = itm.id })" class="viewDialog" data-dialog-title="Удаление производителя"><span class=" glyphicon glyphicon-remove" style="color:red"></span></a></td>*@
        </tr>
    }
</table>
@*<a href="@Url.Action("CreateMan", "SIZ", new { })" class="viewDialog" data-dialog-title="Добавление производителя"><span class=" glyphicon glyphicon-plus"></span></a>*@


